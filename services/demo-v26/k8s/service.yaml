apiVersion: v1
kind: Service
metadata:
  name: demo-v26-service
  namespace: demo-v26
  labels:
    app: demo-v26
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v26
