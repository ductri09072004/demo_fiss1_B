apiVersion: v1
kind: Service
metadata:
  name: demo-v113-service
  namespace: demo-v113
  labels:
    app: demo-v113
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v113
