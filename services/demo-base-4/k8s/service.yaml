apiVersion: v1
kind: Service
metadata:
  name: demo-base-4-service
  namespace: demo-base-4
  labels:
    app: demo-base-4
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-base-4
