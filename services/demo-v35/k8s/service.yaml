apiVersion: v1
kind: Service
metadata:
  name: demo-v35-service
  namespace: demo-v35
  labels:
    app: demo-v35
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v35
