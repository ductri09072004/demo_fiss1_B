apiVersion: v1
kind: Service
metadata:
  name: demo-base-1-service
  namespace: demo-base-1
  labels:
    app: demo-base-1
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5006
    protocol: TCP
    name: http
  selector:
    app: demo-base-1
