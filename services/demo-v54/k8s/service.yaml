apiVersion: v1
kind: Service
metadata:
  name: demo-v54-service
  namespace: demo-v54
  labels:
    app: demo-v54
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: demo-v54
