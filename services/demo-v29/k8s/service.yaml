apiVersion: v1
kind: Service
metadata:
  name: demo-v29-service
  namespace: demo-v29
  labels:
    app: demo-v29
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v29
