apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: node-v3-ingress
  namespace: node-v3
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: gateway.local
    http:
      paths:
      - path: /api/node-v3
        pathType: Prefix
        backend:
          service:
            name: node-v3-service
            port:
              number: 80
