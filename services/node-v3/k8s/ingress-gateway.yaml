apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: node-v3-gateway
  namespace: node-v3
  annotations:
    # regex: /api/node-v3/api/<rest> -> /api/<rest>
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: "/api/$2"
spec:
  ingressClassName: nginx
  rules:
  - host: gateway.local
    http:
      paths:
      - path: /api/node-v3/api(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: node-v3-service
            port:
              number: 80
  - host: localhost
    http:
      paths:
      - path: /api/node-v3/api(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: node-v3-service
            port:
              number: 80
