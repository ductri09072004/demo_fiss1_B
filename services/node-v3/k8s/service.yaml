apiVersion: v1
kind: Service
metadata:
  name: node-v3-service
  namespace: node-v3
  labels:
    app: node-v3
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: node-v3
