apiVersion: v1
kind: Service
metadata:
  name: aemo-v5-service
  namespace: aemo-v5
  labels:
    app: aemo-v5
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: aemo-v5
