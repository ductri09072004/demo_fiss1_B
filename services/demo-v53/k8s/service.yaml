apiVersion: v1
kind: Service
metadata:
  name: demo-v53-service
  namespace: demo-v53
  labels:
    app: demo-v53
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v53
