apiVersion: v1
kind: Service
metadata:
  name: demo-v72-service
  namespace: demo-v72
  labels:
    app: demo-v72
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v72
