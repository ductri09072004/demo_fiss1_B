apiVersion: v1
kind: Service
metadata:
  name: aemo-v4RR-service
  namespace: aemo-v4RR
  labels:
    app: aemo-v4RR
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: aemo-v4RR
