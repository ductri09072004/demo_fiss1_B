apiVersion: v1
kind: Service
metadata:
  name: demo-v13-service
  namespace: demo-v13
  labels:
    app: demo-v13
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v13
