apiVersion: v1
kind: Service
metadata:
  name: demo-v64-service
  namespace: demo-v64
  labels:
    app: demo-v64
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5010
    protocol: TCP
    name: http
  selector:
    app: demo-v64
