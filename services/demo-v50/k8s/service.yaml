apiVersion: v1
kind: Service
metadata:
  name: demo-v50-service
  namespace: demo-v50
  labels:
    app: demo-v50
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: demo-v50
