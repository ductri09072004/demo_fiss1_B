apiVersion: v1
kind: Service
metadata:
  name: demo-v70-service
  namespace: demo-v70
  labels:
    app: demo-v70
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v70
