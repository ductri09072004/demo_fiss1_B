apiVersion: v1
kind: Service
metadata:
  name: demo-4.5-service
  namespace: demo-4.5
  labels:
    app: demo-4.5
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-4.5
