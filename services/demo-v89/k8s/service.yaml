apiVersion: v1
kind: Service
metadata:
  name: demo-v89-service
  namespace: demo-v89
  labels:
    app: demo-v89
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v89
