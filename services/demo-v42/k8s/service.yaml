apiVersion: v1
kind: Service
metadata:
  name: demo-v42-service
  namespace: demo-v42
  labels:
    app: demo-v42
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v42
