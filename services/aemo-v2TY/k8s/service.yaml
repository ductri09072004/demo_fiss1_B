apiVersion: v1
kind: Service
metadata:
  name: aemo-v2TY-service
  namespace: aemo-v2TY
  labels:
    app: aemo-v2TY
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 7000
    protocol: TCP
    name: http
  selector:
    app: aemo-v2TY
