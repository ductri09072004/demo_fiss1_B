apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aemo-v2TY-ingress
  namespace: aemo-v2TY
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: gateway.local
    http:
      paths:
      - path: /api/aemo-v2TY
        pathType: Prefix
        backend:
          service:
            name: aemo-v2TY-service
            port:
              number: 80
