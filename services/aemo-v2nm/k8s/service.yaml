apiVersion: v1
kind: Service
metadata:
  name: aemo-v2nm-service
  namespace: aemo-v2nm
  labels:
    app: aemo-v2nm
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 7000
    protocol: TCP
    name: http
  selector:
    app: aemo-v2nm
