apiVersion: v1
kind: Service
metadata:
  name: demo-v23-service
  namespace: demo-v23
  labels:
    app: demo-v23
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v23
