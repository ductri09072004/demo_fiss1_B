apiVersion: v1
kind: Service
metadata:
  name: demo-v65-service
  namespace: demo-v65
  labels:
    app: demo-v65
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5050
    protocol: TCP
    name: http
  selector:
    app: demo-v65
