apiVersion: v1
kind: Service
metadata:
  name: demo-v15-service
  namespace: demo-v15
  labels:
    app: demo-v15
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v15
