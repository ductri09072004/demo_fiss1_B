apiVersion: v1
kind: Service
metadata:
  name: demo-v14-service
  namespace: demo-v14
  labels:
    app: demo-v14
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v14
