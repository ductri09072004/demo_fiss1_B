apiVersion: v1
kind: Service
metadata:
  name: demo-v79-service
  namespace: demo-v79
  labels:
    app: demo-v79
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5011
    protocol: TCP
    name: http
  selector:
    app: demo-v79
