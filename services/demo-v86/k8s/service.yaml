apiVersion: v1
kind: Service
metadata:
  name: demo-v86-service
  namespace: demo-v86
  labels:
    app: demo-v86
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v86
