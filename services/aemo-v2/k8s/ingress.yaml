apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aemo-v2u-ingress
  namespace: aemo-v2u
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: gateway.local
    http:
      paths:
      - path: /api/aemo-v2u
        pathType: Prefix
        backend:
          service:
            name: aemo-v2u-service
            port:
              number: 80
