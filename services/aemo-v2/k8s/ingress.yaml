apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aemo-v2d-ingress
  namespace: aemo-v2d
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: gateway.local
    http:
      paths:
      - path: /api/aemo-v2d
        pathType: Prefix
        backend:
          service:
            name: aemo-v2d-service
            port:
              number: 80
