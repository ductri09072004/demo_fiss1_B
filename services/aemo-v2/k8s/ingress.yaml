apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aemo-v2-ingress
  namespace: aemo-v2
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: localhost
    http:
      paths:
      - path: /api/aemo-v2
        pathType: Prefix
        backend:
          service:
            name: aemo-v2-service
            port:
              number: 80
