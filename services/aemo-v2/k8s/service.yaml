apiVersion: v1
kind: Service
metadata:
  name: aemo-v2e-service
  namespace: aemo-v2e
  labels:
    app: aemo-v2e
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 7000
    protocol: TCP
    name: http
  selector:
    app: aemo-v2e
