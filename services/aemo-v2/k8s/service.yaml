apiVersion: v1
kind: Service
metadata:
  name: aemo-v2g-service
  namespace: aemo-v2g
  labels:
    app: aemo-v2g
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 7000
    protocol: TCP
    name: http
  selector:
    app: aemo-v2g
