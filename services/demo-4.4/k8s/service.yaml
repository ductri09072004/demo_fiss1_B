apiVersion: v1
kind: Service
metadata:
  name: demo-4.4-service
  namespace: demo-4.4
  labels:
    app: demo-4.4
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-4.4
