apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aka-75-gateway
  namespace: aka-75
  annotations:
    # regex: /api/aka-75/api/<rest> -> /api/<rest>
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: "/api/$2"
spec:
  ingressClassName: nginx
  rules:
  - host: gateway.local
    http:
      paths:
      - path: /api/aka-75/api(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: aka-75-service
            port:
              number: 80
  - host: localhost
    http:
      paths:
      - path: /api/aka-75/api(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: aka-75-service
            port:
              number: 80
