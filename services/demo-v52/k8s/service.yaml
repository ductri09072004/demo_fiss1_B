apiVersion: v1
kind: Service
metadata:
  name: demo-v52-service
  namespace: demo-v52
  labels:
    app: demo-v52
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: demo-v52
