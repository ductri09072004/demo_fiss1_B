apiVersion: v1
kind: Service
metadata:
  name: demo-v44-service
  namespace: demo-v44
  labels:
    app: demo-v44
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v44
