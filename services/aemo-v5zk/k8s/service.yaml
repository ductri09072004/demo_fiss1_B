apiVersion: v1
kind: Service
metadata:
  name: aemo-v5zk-service
  namespace: aemo-v5zk
  labels:
    app: aemo-v5zk
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: aemo-v5zk
