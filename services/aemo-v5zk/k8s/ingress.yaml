apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aemo-v5zk-ingress
  namespace: aemo-v5zk
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: gateway.local
    http:
      paths:
      - path: /api/aemo-v5zk
        pathType: Prefix
        backend:
          service:
            name: aemo-v5zk-service
            port:
              number: 80
