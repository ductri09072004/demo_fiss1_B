apiVersion: v1
kind: Service
metadata:
  name: aemo-v4PP-service
  namespace: aemo-v4PP
  labels:
    app: aemo-v4PP
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: aemo-v4PP
