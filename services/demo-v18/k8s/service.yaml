apiVersion: v1
kind: Service
metadata:
  name: demo-v18-service
  namespace: demo-v18
  labels:
    app: demo-v18
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v18
