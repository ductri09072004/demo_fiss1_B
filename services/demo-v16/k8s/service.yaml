apiVersion: v1
kind: Service
metadata:
  name: demo-v16-service
  namespace: demo-v16
  labels:
    app: demo-v16
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v16
