apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: zemo-v4-gateway
  namespace: zemo-v4
  annotations:
    # regex: /api/zemo-v4/api/<rest> -> /api/<rest>
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: "/api/$2"
spec:
  ingressClassName: nginx
  rules:
  - host: gateway.local
    http:
      paths:
      - path: /api/zemo-v4/api(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: zemo-v4-service
            port:
              number: 80
  - host: localhost
    http:
      paths:
      - path: /api/zemo-v4/api(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: zemo-v4-service
            port:
              number: 80
