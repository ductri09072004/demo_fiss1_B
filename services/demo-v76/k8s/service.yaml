apiVersion: v1
kind: Service
metadata:
  name: demo-v76-service
  namespace: demo-v76
  labels:
    app: demo-v76
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v76
