apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-v76-gateway
  namespace: demo-v76
  annotations:
    # regex: /api/demo-v76/api/<rest> -> /api/<rest>
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: "/api/$2"
spec:
  ingressClassName: nginx
  rules:
  - host: gateway.local
    http:
      paths:
      - path: /api/demo-v76/api(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: demo-v76-service
            port:
              number: 80
  - host: localhost
    http:
      paths:
      - path: /api/demo-v76/api(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: demo-v76-service
            port:
              number: 80
