apiVersion: v1
kind: Service
metadata:
  name: demo-base-2-service
  namespace: demo-base-2
  labels:
    app: demo-base-2
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-base-2
