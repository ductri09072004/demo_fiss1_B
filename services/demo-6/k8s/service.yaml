apiVersion: v1
kind: Service
metadata:
  name: demo-6-service
  namespace: demo-6
  labels:
    app: demo-6
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-6
