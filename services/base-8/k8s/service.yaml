apiVersion: v1
kind: Service
metadata:
  name: base-8-service
  namespace: base-8
  labels:
    app: base-8
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: base-8
