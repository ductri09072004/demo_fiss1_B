apiVersion: v1
kind: Service
metadata:
  name: demo-v17-service
  namespace: demo-v17
  labels:
    app: demo-v17
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v17
