apiVersion: v1
kind: Service
metadata:
  name: demo-v115-service
  namespace: demo-v115
  labels:
    app: demo-v115
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v115
