apiVersion: v1
kind: Service
metadata:
  name: demo-v48-service
  namespace: demo-v48
  labels:
    app: demo-v48
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: demo-v48
