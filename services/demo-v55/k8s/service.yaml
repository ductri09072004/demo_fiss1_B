apiVersion: v1
kind: Service
metadata:
  name: demo-v55-service
  namespace: demo-v55
  labels:
    app: demo-v55
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v55
