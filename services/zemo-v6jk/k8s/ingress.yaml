apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: zemo-v6jk-ingress
  namespace: zemo-v6jk
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: gateway.local
    http:
      paths:
      - path: /api/zemo-v6jk
        pathType: Prefix
        backend:
          service:
            name: zemo-v6jk-service
            port:
              number: 80
