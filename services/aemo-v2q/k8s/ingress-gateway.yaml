apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aemo-v2q-gateway
  namespace: aemo-v2q
  annotations:
    # regex: /api/aemo-v2q/api/<rest> -> /api/<rest>
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: "/api/$2"
spec:
  ingressClassName: nginx
  rules:
  - host: gateway.local
    http:
      paths:
      - path: /api/aemo-v2q/api(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: aemo-v2q-service
            port:
              number: 80
  - host: localhost
    http:
      paths:
      - path: /api/aemo-v2q/api(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: aemo-v2q-service
            port:
              number: 80
