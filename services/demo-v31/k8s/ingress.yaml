apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-v31-ingress
  namespace: demo-v31
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    external-dns.alpha.kubernetes.io/hostname: jollicow.store
spec:
  tls:
  - hosts:
    - jollicow.store
    secretName: demo-v31-tls
  rules:
  - host: jollicow.store
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: demo-v31-service
            port:
              number: 80
