apiVersion: v1
kind: Service
metadata:
  name: demo-v31-service
  namespace: demo-v31
  labels:
    app: demo-v31
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v31
