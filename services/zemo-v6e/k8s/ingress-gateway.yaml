apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: zemo-v6e-gateway
  namespace: zemo-v6e
  annotations:
    # regex: /api/zemo-v6e/api/<rest> -> /api/<rest>
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: "/api/$2"
spec:
  ingressClassName: nginx
  rules:
  - host: gateway.local
    http:
      paths:
      - path: /api/zemo-v6e/api(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: zemo-v6e-service
            port:
              number: 80
  - host: localhost
    http:
      paths:
      - path: /api/zemo-v6e/api(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: zemo-v6e-service
            port:
              number: 80
