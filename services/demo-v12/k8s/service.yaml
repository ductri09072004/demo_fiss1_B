apiVersion: v1
kind: Service
metadata:
  name: demo-v12-service
  namespace: demo-v12
  labels:
    app: demo-v12
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v12
