apiVersion: v1
kind: Service
metadata:
  name: demo-v25-service
  namespace: demo-v25
  labels:
    app: demo-v25
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v25
