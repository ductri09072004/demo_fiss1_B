apiVersion: v1
kind: Service
metadata:
  name: demo-v41-service
  namespace: demo-v41
  labels:
    app: demo-v41
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v41
