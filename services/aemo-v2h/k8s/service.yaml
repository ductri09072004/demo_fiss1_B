apiVersion: v1
kind: Service
metadata:
  name: aemo-v2h-service
  namespace: aemo-v2h
  labels:
    app: aemo-v2h
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 7000
    protocol: TCP
    name: http
  selector:
    app: aemo-v2h
