apiVersion: v1
kind: Service
metadata:
  name: demo-v39-service
  namespace: demo-v39
  labels:
    app: demo-v39
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v39
