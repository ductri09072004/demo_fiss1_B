apiVersion: v1
kind: Service
metadata:
  name: demo-v20-service
  namespace: demo-v20
  labels:
    app: demo-v20
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v20
