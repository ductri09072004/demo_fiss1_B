apiVersion: v1
kind: Service
metadata:
  name: demo-v22-service
  namespace: demo-v22
  labels:
    app: demo-v22
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v22
