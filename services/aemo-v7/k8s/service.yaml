apiVersion: v1
kind: Service
metadata:
  name: aemo-v7-service
  namespace: aemo-v7
  labels:
    app: aemo-v7
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 7000
    protocol: TCP
    name: http
  selector:
    app: aemo-v7
