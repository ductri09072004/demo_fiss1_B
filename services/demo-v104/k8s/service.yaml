apiVersion: v1
kind: Service
metadata:
  name: demo-v104-service
  namespace: demo-v104
  labels:
    app: demo-v104
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v104
