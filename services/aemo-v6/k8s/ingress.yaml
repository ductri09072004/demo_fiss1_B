apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aemo-v6-ingress
  namespace: aemo-v6
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: gateway.local
    http:
      paths:
      - path: /api/aemo-v6
        pathType: Prefix
        backend:
          service:
            name: aemo-v6-service
            port:
              number: 80
