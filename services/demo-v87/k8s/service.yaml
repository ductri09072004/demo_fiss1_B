apiVersion: v1
kind: Service
metadata:
  name: demo-v87-service
  namespace: demo-v87
  labels:
    app: demo-v87
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v87
