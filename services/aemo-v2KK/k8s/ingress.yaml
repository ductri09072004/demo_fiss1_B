apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aemo-v2KK-ingress
  namespace: aemo-v2KK
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: gateway.local
    http:
      paths:
      - path: /api/aemo-v2KK
        pathType: Prefix
        backend:
          service:
            name: aemo-v2KK-service
            port:
              number: 80
