apiVersion: v1
kind: Service
metadata:
  name: demo-v116-service
  namespace: demo-v116
  labels:
    app: demo-v116
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v116
