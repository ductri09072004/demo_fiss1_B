apiVersion: v1
kind: Service
metadata:
  name: demo-v19-service
  namespace: demo-v19
  labels:
    app: demo-v19
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v19
