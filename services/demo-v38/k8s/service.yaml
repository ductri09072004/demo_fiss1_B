apiVersion: v1
kind: Service
metadata:
  name: demo-v38-service
  namespace: demo-v38
  labels:
    app: demo-v38
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v38
