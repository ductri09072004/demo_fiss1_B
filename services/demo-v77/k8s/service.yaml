apiVersion: v1
kind: Service
metadata:
  name: demo-v77-service
  namespace: demo-v77
  labels:
    app: demo-v77
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v77
