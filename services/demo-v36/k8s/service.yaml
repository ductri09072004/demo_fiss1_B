apiVersion: v1
kind: Service
metadata:
  name: demo-v36-service
  namespace: demo-v36
  labels:
    app: demo-v36
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v36
