apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aemo-v4NF-ingress
  namespace: aemo-v4NF
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: gateway.local
    http:
      paths:
      - path: /api/aemo-v4NF
        pathType: Prefix
        backend:
          service:
            name: aemo-v4NF-service
            port:
              number: 80
