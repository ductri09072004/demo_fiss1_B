apiVersion: v1
kind: Service
metadata:
  name: demo-v108-service
  namespace: demo-v108
  labels:
    app: demo-v108
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v108
