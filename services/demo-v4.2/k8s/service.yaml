apiVersion: v1
kind: Service
metadata:
  name: demo-v4.2-service
  namespace: demo-v4.2
  labels:
    app: demo-v4.2
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v4.2
