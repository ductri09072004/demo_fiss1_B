apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-v61-gateway
  namespace: demo-v61
  annotations:
    # regex: /api/demo-v61/api/<rest> -> /api/<rest>
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: "/api/$2"
spec:
  ingressClassName: nginx
  rules:
  - host: gateway.local
    http:
      paths:
      - path: /api/demo-v61/api(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: demo-v61-service
            port:
              number: 80
  - host: localhost
    http:
      paths:
      - path: /api/demo-v61/api(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: demo-v61-service
            port:
              number: 80
