apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: base-5-gateway
  namespace: base-5
  annotations:
    # regex: /api/base-5/api/<rest> -> /api/<rest>
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: "/api/$2"
spec:
  ingressClassName: nginx
  rules:
  - host: gateway.local
    http:
      paths:
      - path: /api/base-5/api(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: base-5-service
            port:
              number: 80
  - host: localhost
    http:
      paths:
      - path: /api/base-5/api(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: base-5-service
            port:
              number: 80
