apiVersion: v1
kind: Service
metadata:
  name: base-5-service
  namespace: base-5
  labels:
    app: base-5
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: base-5
