apiVersion: v1
kind: Service
metadata:
  name: demo-v105-service
  namespace: demo-v105
  labels:
    app: demo-v105
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v105
