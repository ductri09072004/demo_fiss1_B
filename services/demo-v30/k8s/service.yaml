apiVersion: v1
kind: Service
metadata:
  name: demo-v30-service
  namespace: demo-v30
  labels:
    app: demo-v30
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v30
