apiVersion: v1
kind: Service
metadata:
  name: aemo-v4ZX-service
  namespace: aemo-v4ZX
  labels:
    app: aemo-v4ZX
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: aemo-v4ZX
