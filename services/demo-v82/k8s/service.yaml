apiVersion: v1
kind: Service
metadata:
  name: demo-v82-service
  namespace: demo-v82
  labels:
    app: demo-v82
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: demo-v82
