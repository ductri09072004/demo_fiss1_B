apiVersion: v1
kind: Service
metadata:
  name: demo-v49-service
  namespace: demo-v49
  labels:
    app: demo-v49
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v49
