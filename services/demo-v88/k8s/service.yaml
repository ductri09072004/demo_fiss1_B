apiVersion: v1
kind: Service
metadata:
  name: demo-v88-service
  namespace: demo-v88
  labels:
    app: demo-v88
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v88
