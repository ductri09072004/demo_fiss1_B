apiVersion: v1
kind: Service
metadata:
  name: demo-v88-service
  namespace: demo-v88
  labels:
    app: demo-v88
spec:
  type: ClusterIP
  selector:
    app: demo-v88
  ports:
  - port: 5001
    targetPort: 5001
    protocol: TCP
