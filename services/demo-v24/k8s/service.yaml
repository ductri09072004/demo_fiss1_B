apiVersion: v1
kind: Service
metadata:
  name: demo-v24-service
  namespace: demo-v24
  labels:
    app: demo-v24
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: demo-v24
