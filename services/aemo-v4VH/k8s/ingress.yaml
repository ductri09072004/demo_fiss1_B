apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aemo-v4VH-ingress
  namespace: aemo-v4VH
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: gateway.local
    http:
      paths:
      - path: /api/aemo-v4VH
        pathType: Prefix
        backend:
          service:
            name: aemo-v4VH-service
            port:
              number: 80
